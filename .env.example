# Copy to .env and fill in. .env is gitignored; keep it chmod 600.

# allkeys.directory API key, used by --upload
ALLKEYS_API_KEY=ak_...

# Optional: blockchain.info API key, raises the lookup rate limit
# BLOCKCHAIN_API_KEY=
